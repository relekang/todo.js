# @relekang/todo-gist-backend

> Gist based storage backend for @relekang/todo

## Usage

```
npm i -g @relekang/todo-gist-backend
```

Add profile with type `gist`. Example config:

```yaml
profiles:
  default:
    type: gist
    id: the-id
    token: personal-access-token-for-gists
```

